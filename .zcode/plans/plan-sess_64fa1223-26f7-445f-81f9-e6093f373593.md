1. Tighten LLM authoring with a bounded duration-repair loop in `C:\Projects\bevy-infinite\crates\backlot-llm\src\author.rs`.
   - Keep the existing structure: plan request, per-beat requests, validation, truthful fallback semantics.
   - After all beat commands are generated and validated, build a temporary `PlannedEpisode` and estimate authored runtime from the validated actions using the existing duration primitives from `backlot_core::validation`.
   - Add a bounded repair pass keyed off `DirectorConfig.max_repairs`:
     - If estimated runtime is materially below the requested 45–60s window, re-prompt the model with explicit feedback about the shortfall and ask for a repaired `EpisodePlan` and/or repaired beat commands.
     - Preserve `require_llm` semantics: if the model cannot repair into range, fail clearly instead of silently accepting a short LLM plan.
     - Preserve truthful provenance by marking repair use in `PlanAuthorship` / `BeatAuthorship` and using a more specific `validation_status` string such as `duration_repaired` or `duration_out_of_range`.
   - Keep this loop in `backlot-llm` rather than `backlot-core` so LLM-specific logic stays localized and the runtime/render pipeline stays engine-agnostic.

2. Add a hard pre-render duration gate in `C:\Projects\bevy-infinite\crates\backlot-core\src\render.rs` inside `prepare_production()`.
   - Right after `build_schedule(...)` and `compact_dead_air(...)`, compare `sched.duration` against the configured target and the user-required acceptance band.
   - If the schedule is still too short after real TTS measurement and dead-air compaction:
     - in `require_llm` mode, return a clear error that the authored episode did not meet runtime requirements;
     - otherwise keep current non-fatal behavior only if that remains consistent with existing fallback mode.
   - This gives a truthful final barrier based on the authoritative timeline instead of only the author-side estimate.
   - Reuse existing metrics already produced there: `sched.duration`, dialogue timings, gap analysis, and measured TTS durations.

3. Replace the fragile Bevy startup pre-roll in `C:\Projects\bevy-infinite\crates\backlot-app\src\bevy_capture.rs` with renderer-readiness gating.
   - Remove the current block that:
     - installs lenient `FallbackErrorHandler`s,
     - repeatedly calls `app.update()` inside `catch_unwind`,
     - mirrors `RenderDevice` from the render world into the main world.
   - Replace it with a small readiness resource/state and gate capture progression until the Bevy renderer is actually ready.
   - Keep the WARP/DX12 `RenderPlugin` configuration already added, since that part correctly selects Microsoft Basic Render Driver in this environment.
   - If readiness gating alone does not prevent first-frame resource validation failures, defer spawning the PBR-heavy scene content until readiness instead of forcing startup through panics.
   - This is the smallest code change likely to stabilize the real Bevy render path without inventing a second renderer.

4. Run focused verification for the repaired authoring and Bevy path.
   - LLM authoring verification:
     - run the one-shot producer in `--require-llm` mode against the user’s now-running local server;
     - confirm failures are fatal when the plan stays short;
     - confirm a successful plan reports LLM provenance truthfully.
   - Duration verification:
     - inspect the produced report/diagnostics and confirm `sched.duration` lands inside the target band after measured TTS and compaction.
   - Bevy verification:
     - run `--render-backend bevy --produce-one --require-llm` and verify the capture path reaches frame production instead of failing during renderer startup.
   - Report results truthfully: whether the code is complete, whether the sandbox can render the final artifact here, and any remaining user-side execution only.

5. Defer the larger visual upgrades until after the duration and Bevy-authoritative path are unblocked.
   - The repo still needs better performers, a more readable apartment hallway/elevator set, and stronger camera rejection/coverage to satisfy the full goal.
   - But the next execution step should first restore a truthful LLM-authored, duration-compliant, Bevy-rendered pipeline, because those are the gating failures right now.
   - Once that path works, continue with performer/set/camera upgrades against the same authoritative timeline and diagnostics.

Planned file touch set for the first implementation pass:
- `C:\Projects\bevy-infinite\crates\backlot-llm\src\author.rs`
- `C:\Projects\bevy-infinite\crates\backlot-core\src\render.rs`
- `C:\Projects\bevy-infinite\crates\backlot-app\src\bevy_capture.rs`

Verification commands to request permission for during implementation:
- run Cargo tests for the touched crates
- run one-shot production with `--produce-one --require-llm --render-backend cpu`
- run one-shot production with `--produce-one --require-llm --render-backend bevy`